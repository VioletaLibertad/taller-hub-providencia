import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  card: {
    marginTop: 5,
    marginLeft: 10,
    marginRight: 10,
  },
  image: {
    flex: 1,
    height: 200,
    width: '100%'
  },
})

export default styles;
